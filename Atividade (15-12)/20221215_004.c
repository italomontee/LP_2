#include <stdio.h>

int main()
{
    float pi;
    float raio;



    printf("valor de pi:");
    scanf("%f",&pi);

    printf("valor do raio:");
    scanf("%f",&raio);

     printf("\n a area desse circulo e igual a= %.1f", pi*raio*raio);

    return 0;
}
